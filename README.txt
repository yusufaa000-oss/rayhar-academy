RAYHAR ACADEMY BACKEND V1
Registration + Login + Student Database

Use Supabase as the cloud backend. Run database.sql in Supabase SQL Editor.
Put your Project URL and publishable/anon key in config.js.
NEVER put a service_role/secret key in website code.
Then upload config.js and student-auth.html to GitHub Pages.
